export interface PaisInterfaces{
    id:number;
    nombre:string;
    // ruta_icon:string;
    // ruta_logo:string;
}

export interface IdentificadorInterfaces{
    paisId:number;
    identificador:string;
}

export interface PaisLogoInterfaces{
    paisLogoId:number;
    ruta:string;
}